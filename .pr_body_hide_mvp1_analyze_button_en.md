## Summary

- Hides the MVP1 standalone bank statement analysis button in the frontend form.
- Keeps the underlying feature code intact for possible future reactivation.
- Leaves the reconciliation submit flow unchanged.

## Why

The MVP1 "Analyze statement (preview)" action should be temporarily removed from the visible UI while product direction is being decided, without deleting existing implementation.

## Type of change

- [ ] feat
- [ ] fix
- [ ] docs
- [ ] refactor
- [x] chore
- [ ] test

## How to test

1. Verify the HTML update:
   - `rg -n "analyze-btn" frontend/index.html`
   - Expected line includes `hidden` in the `analyze-btn` button.
2. Open the frontend page and confirm only "Enviar para conciliação" appears in actions.
3. Confirm no backend/API behavior changed by this PR.

## Data / storage impact

- [x] No data/storage changes
- [ ] Temporary storage behavior changed
- [ ] Persistent schema changed
- [ ] Requires manual operation

If manual operation is required, describe it:

N/A

## Checklist

- [x] Conventional Commit used
- [x] Code reviewed locally
- [ ] Tests added/updated when relevant
- [ ] Docs updated when relevant
- [x] Backward-compatibility considered
- [ ] API endpoints validated manually (`POST /analyze`, `GET /report/{analysis_id}`)
- [x] Reconciliation behavior validated when changed

## Screenshots / logs (optional)

- `rg -n "analyze-btn" frontend/index.html`
- Output: `37:          <button id="analyze-btn" type="button" hidden>Analisar extrato (preview)</button>`

## Risks and rollback

- Risk level: low
- Rollback plan:
  - Revert this PR commit to show the button again.
